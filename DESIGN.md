# DESIGN.md · Dan 个人名片（票根世界 · 方向 C）

## 1. 方向契约
- **场景**：北京开发者大会展馆强光下，手机举在手臂距离，10 秒扫读后加微信。
- **策略**：Committed。纸白票根拥有地面，酒红只做证件条、印章与关键动作。
- **批准**：用户从三个原型（地铁线 / 实验本 / 票根）中选定 C，原型 `.impeccable/mocks/dirC-lanyard-pass.png`。
- **拒绝**：深色夜光模板、奶油暖底、渐变字、毛玻璃、每节 eyebrow 小帽、数字编号脚手架、大数字英雄区。
- **用户指定的弹窗**：项目截图收进原生 dialog（用户明确要求，覆盖之前的不弹窗决定）。点票根开弹窗，点背景、✕ 或 ESC 关闭；GET 钮直跳 App Store，不进弹窗。

## 2. 色彩（OKLCH，全文件无 hex）
- 页面纸 `oklch(0.957 0.008 343)`，票根纯白 `oklch(1 0 0)`，域底 `oklch(0.97 0.008 343)`。
- 正文墨 `oklch(0.24 0.035 345)`，次文 `oklch(0.47 0.03 345)`。
- 酒红 `oklch(0.58 0.19 343)`（白字），深 `oklch(0.5 0.175 343)`；信息蓝 `oklch(0.47 0.12 235)`（白字，用于 R&D 章、SIDE 章、FDE 徽章）。
- 全站只留一种绿：微信绿 `oklch(0.65 0.18 155)`，且只用在加微信相关元素（ADD 章、小绿点）——看到绿就是联系方式。
- 动作钮统一用墨章黑 `oklch(0.24 0.035 345)`（白字，按压时增亮）；微信绿 `oklch(0.65 0.18 155)` 只做 8px 圆点点缀与 ADD 小章，不再做大面积按钮底。
- 线 `oklch(0.84 0.018 343)`，撕口虚线 `oklch(0.72 0.03 343)`；投影带偏移加柔边。

## 3. 结构语法
- 顶部挂绳 SVG ＋ 打孔徽章（DAN 大字、酒红会议条、三枚身份章、slogan、黑标头像）。
- 票根=分区：B2B 企业票（规格表）、APP 票（GET 验真钮）、双联副项目（左右不同图文）、课程票（ ruled 条加云标签）、破冰票、二维码末联。
- 侧面齿孔用背景色圆盖出，撕口用虚线；双联内部用虚线分隔，窄屏自动上下叠。
- 首屏票根只露标题行（一行标题加一行灰色副文）；截图与规格表收进弹窗，弹窗内可放多张截图。唯一的编排动效是 Toast 上浮（指数缓出），减弱动效时关闭过渡。
